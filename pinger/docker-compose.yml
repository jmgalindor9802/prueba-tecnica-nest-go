services:
  pinger:
    build: .
    cap_add:
      - NET_RAW
    volumes:
      - ./targets.txt:/targets.txt:ro
    command:
      - "-workers"
      - "${WORKERS:-100}"
      - "-timeout"
      - "${TIMEOUT:-1s}"
      - "-count"
      - "${COUNT:-3}"
    ports:
      - "8080:8080"
